services:
  app:
    build:
      context: .
      args:
        PYTHON_VERSION: "3.8"
    image: protwis-django:dev
    container_name: protwis-app
    restart: unless-stopped
    environment:
      - DJANGO_SETTINGS_MODULE=protwis.settings_local_development
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
      - POSTGRES_USER=protwis
      - POSTGRES_PASSWORD=protwis
      - POSTGRES_DB=protwis
    # Source and data are bind-mounted — the image stays code-free so
    # upgrading Python/Django is a pure image rebuild.
    # Override defaults by setting PROTWIS_SRC / PROTWIS_DATA env vars.
    volumes:
      - ${PROTWIS_SRC:-../protwis}:/app/src
      - ${PROTWIS_DATA:-../protwis/data}:/app/data
    ports:
      - "8000:8000"
    networks:
      - gpcrdb
    depends_on:
      db:
        condition: service_healthy

  db:
    image: iskoldt/postgres16-rdkit:latest
    container_name: protwis-db
    restart: unless-stopped
    shm_size: 2g
    environment:
      - POSTGRES_USER=protwis
      - POSTGRES_PASSWORD=protwis
      - POSTGRES_DB=protwis
      - PGDATA=/var/lib/postgresql/data
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - gpcrdb
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U protwis"]
      interval: 10s
      timeout: 5s
      retries: 5

  adminer:
    image: adminer
    container_name: protwis-adminer
    restart: unless-stopped
    ports:
      - "8888:8080"
    environment:
      - ADMINER_DEFAULT_SERVER=db
    networks:
      - gpcrdb
    depends_on:
      db:
        condition: service_healthy

networks:
  gpcrdb:

volumes:
  postgres_data:
